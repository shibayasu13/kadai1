<h1>Editing Book</h1>

	<%= form_for(@book) do |f| %>
		<h4>Title<h4>
	 	<%= f.text_area :title %>
		<h4>Body<h4>
	 	<%= f.text_area :body %>
	 	<%= f.submit '保存' %>
	<% end %>


<%= link_to 'Show', @book, class: "show_#{@book.id}" %> |
<%= link_to 'Back', books_path, class: "back" %>
